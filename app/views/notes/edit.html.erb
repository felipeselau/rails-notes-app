<% content_for :title, "Editing note" %>

<h1>Editing note</h1>
<div class="card" style="margin: 12px 0 16px;">
  <%= render "form", note: @note %>
  </div>
<div style="display:flex; gap: 10px; flex-wrap: wrap;">
  <%= link_to "Show note", @note, class: "btn" %>
  <%= link_to "Back to notes", notes_path, class: "btn" %>
  </div>
