<% content_for :title, "Editing list" %>

<h1>Editing list</h1>
<div class="card" style="margin: 12px 0 16px;">
  <%= render "form", list: @list %>
  </div>
<div style="display:flex; gap: 10px; flex-wrap: wrap;">
  <%= link_to "Show list", @list, class: "btn" %>
  <%= link_to "Back to lists", lists_path, class: "btn" %>
  </div>
